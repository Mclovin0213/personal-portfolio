import React from 'react';

const Hero: React.FC = () => {
  return (
    <section id="hero">
      <h1>Software Engineer & Creator</h1>
      {/* Add social links here */}
    </section>
  );
};

export default Hero;
